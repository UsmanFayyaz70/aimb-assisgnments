// Name Cases: Store a person’s name in a variable, and then print that person’s name in lowercase,
// uppercase, and titlecase.
function question3() {
    var fullName = "USMAN FAYYAZ";
    console.log("Lowered Case: ".concat(fullName.toLowerCase()));
    console.log("Upper Case: ".concat(fullName.toUpperCase()));
}
question3();
